PACKAGE = trilinos
PACKAGEVERSION = 17.1.0
MODE = mpi

EXISTENCETEST = --title "tpetra header" --dir inc Tpetra_Core.hpp

CMAKETEST = --title "can we compile" has.c
CMAKETEST = --title "do we have kokkos" kokkos_run.cpp

exit

../cmake_test_driver.sh ${standardflags} -l ${logfile} \
			--title "can we compile" \
			has.c

../cmake_test_driver.sh ${standardflags} -l ${logfile} \
			--title "do we have kokkos" \
			kokkos_run.cpp

