####
####
mkdir -p pylauncher_output && cd pylauncher_output && touch PYL_ID && ../randomsleep 10 20
mkdir -p pylauncher_output && cd pylauncher_output && touch PYL_ID && ../randomsleep 10 20
mkdir -p pylauncher_output && cd pylauncher_output && touch PYL_ID && ../randomsleep 10 20
mkdir -p pylauncher_output && cd pylauncher_output && touch PYL_ID && ../randomsleep 10 20
mkdir -p pylauncher_output && cd pylauncher_output && touch PYL_ID && ../randomsleep 10 20
mkdir -p pylauncher_output && cd pylauncher_output && touch PYL_ID && ../randomsleep 10 20
mkdir -p pylauncher_output && cd pylauncher_output && touch PYL_ID && ../randomsleep 10 20
mkdir -p pylauncher_output && cd pylauncher_output && touch PYL_ID && ../randomsleep 10 20
mkdir -p pylauncher_output && cd pylauncher_output && touch PYL_ID && ../randomsleep 10 20
mkdir -p pylauncher_output && cd pylauncher_output && touch PYL_ID && ../randomsleep 10 20
